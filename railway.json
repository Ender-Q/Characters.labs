{
  "build": {
    "builder": "NIXPKGS",
    "buildCommand": "cd server && npm install && cd ../client_new && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd server && NODE_ENV=production npm start"
  }
}
